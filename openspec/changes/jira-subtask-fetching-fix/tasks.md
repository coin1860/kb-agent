# Tasks

- [x] 1. Rewrite `_format_issue` in `jira.py` to prevent exposing subtask keys and clean up unused fields.
- [x] 2. Remove `write_subtask` call in `_fetch_issue` in `jira.py`.
- [x] 3. Remove `write_subtask` method in `cache.py`.
- [x] 4. Add Jira summarization guidance in `SYNTHESIZE_SYSTEM` inside `nodes.py`.
- [x] 5. Fix `KeyError` bug in `_run_jira_command` inside `tui.py`.
